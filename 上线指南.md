# 托管作业小程序 - 上线操作指南

## 一、云函数上传（必须完成）

### 1. 在 HBuilderX 中关联云服务空间
1. 打开 HBuilderX，确保 `tuoguan-a` 项目已打开
2. 右键点击 `uniCloud-aliyun` 文件夹
3. 选择「关联云服务空间或项目」
4. 选择服务空间：**tuoguan** (SpaceId: mp-f72cbfe6-8460-4535-bb4d-4424acd001f8)
5. 点击确定

### 2. 上传数据库表结构
1. 右键 `uniCloud-aliyun/database` 文件夹
2. 选择「上传所有DB Schema及扩展校验函数」
3. 等待成功提示

### 3. 上传云函数
依次上传三个云函数：
- 右键 `uniCloud-aliyun/cloudfunctions/auth` → 「上传部署」
- 右键 `uniCloud-aliyun/cloudfunctions/students` → 「上传部署」
- 右键 `uniCloud-aliyun/cloudfunctions/homework` → 「上传部署」

### 4. 验证云函数
1. 右键 `uniCloud-aliyun` → 「打开 uniCloud Web 控制台」
2. 在控制台左侧菜单点击「云函数」
3. 确认看到 auth、students、homework 三个云函数

---

## 二、启用真实登录（删除 Mock 代码）

### 修改登录页
打开 `pages/login/login.vue`，删除第 48-63 行的 Mock 登录代码：

```javascript
// 删除这段代码（第 48-63 行）
// ===== 临时 Mock 登录（云函数上传成功后删除这段） =====
setTimeout(() => {
	const mockUser = {
		id: 'mock_user_' + Date.now(),
		role: selectedRole.value,
		name: selectedRole.value === 'teacher' ? '王老师' : '张爸爸',
		phone: '13800138000'
	}
	setToken('mock_token_' + Date.now())
	setUserInfo(mockUser)

	const url = selectedRole.value === 'teacher'
		? '/pages/teacher/home/home'
		: '/pages/parent/home/home'
	uni.reLaunch({ url })
	loading.value = false
}, 500)
return
// ===== Mock 登录结束 =====
```

删除后，重新运行项目，登录将调用真实的微信登录和云函数。

---

## 三、小程序配置检查

### 1. 确认 AppID 和 AppSecret
- AppID: `wxe1ad102c8c75995e`
- AppSecret: `83adb806ecc63ec3d2019352222d3b04`
- 已配置在 `manifest.json` 和 `cloudfunctions/auth/index.js` 中

### 2. 配置服务器域名
登录微信公众平台：https://mp.weixin.qq.com/

进入「开发 → 开发管理 → 开发设置 → 服务器域名」，添加：

**request 合法域名：**
```
https://api.next.bspapp.com
```

**uploadFile 合法域名：**
```
https://file-uninixdjjd-mp-f72cbfe6-8460-4535-bb4d-4424acd001f8.oss-cn-zhangjiakou.aliyuncs.com
```

**downloadFile 合法域名：**
```
https://mp-f72cbfe6-8460-4535-bb4d-4424acd001f8.cdn.bspapp.com
```

---

## 四、功能测试清单

### 老师端测试
- [ ] 登录：选择"老师"身份，能成功登录
- [ ] 添加学生：点击右下角 + 按钮，填写学生信息，保存成功
- [ ] 查看学生列表：能看到刚添加的学生
- [ ] 删除学生：长按学生卡片，选择删除，确认删除成功
- [ ] 记录作业：点击学生卡片，进入记录页面，填写作业信息，保存成功
- [ ] 查看历史记录：切换到"记录"标签，能看到刚才记录的作业

### 家长端测试
- [ ] 登录：选择"家长"身份，能成功登录
- [ ] 绑定孩子：进入"我的"页面，点击"立即绑定"，输入学生姓名
- [ ] 查看作业：在首页能看到孩子今天的作业记录
- [ ] 查看统计：切换到"统计"标签，能看到完成率和趋势图

---

## 五、提交小程序审核

### 1. 在 HBuilderX 中生成上传代码
1. 点击菜单：「发行 → 小程序-微信」
2. 填写版本号（如：1.0.0）和项目备注
3. 点击「发行」，等待编译完成

### 2. 在微信开发者工具中上传代码
1. 打开微信开发者工具
2. 点击右上角「上传」按钮
3. 填写版本号和项目备注
4. 点击「上传」

### 3. 在微信公众平台提交审核
1. 登录 https://mp.weixin.qq.com/
2. 进入「版本管理」
3. 找到刚上传的版本，点击「提交审核」
4. 填写审核信息：
   - 服务类目：教育 → 在线教育
   - 标签：作业、托管、教育
   - 功能页面：选择登录页、老师端首页、家长端首页
5. 提交审核，等待微信审核（通常 1-7 天）

---

## 六、审核通过后发布

1. 审核通过后，在「版本管理」中点击「发布」
2. 发布后，用户可以在微信中搜索小程序名称使用

---

## 七、初始数据准备

### 添加测试学生（老师端操作）
1. 老师登录后，点击右下角 + 按钮
2. 添加几个测试学生：
   - 张小明 / 三年级 / 1班
   - 李小红 / 三年级 / 1班
   - 王小刚 / 四年级 / 2班

### 家长绑定学生
家长登录后，需要手动在云数据库中绑定：
1. 打开 uniCloud Web 控制台
2. 进入「云数据库」→ `students` 表
3. 找到对应的学生记录，编辑 `parent_id` 字段
4. 填入家长的 `_id`（在 `users` 表中查找）

**或者**开发一个"家长输入绑定码"的功能（后续优化）

---

## 八、常见问题

### 问题1：云函数调用失败
- 检查是否已上传云函数
- 检查云服务空间是否关联正确
- 查看云函数日志排查错误

### 问题2：微信登录失败
- 检查 AppSecret 是否正确
- 检查服务器域名是否配置
- 查看云函数 auth 的日志

### 问题3：家长看不到作业
- 检查学生的 parent_id 是否正确绑定
- 检查云函数 homework 的权限逻辑

---

## 九、后续优化建议

1. **家长绑定优化**：开发"绑定码"功能，老师生成绑定码，家长输入绑定
2. **消息推送**：作业记录后推送通知给家长
3. **照片上传**：支持上传作业照片
4. **数据导出**：导出 Excel 报表
5. **多机构支持**：支持多个托管机构独立管理

---

## 联系方式

如有问题，请联系开发者。
